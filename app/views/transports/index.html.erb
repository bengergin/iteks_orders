<% content_for :title do %>Transports<% end %>
<p class="controls">
  <a href="/transports/new">Add new transport</a>
</p>
<table>
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>
  <tbody>
  <% @transports.each do |transport| %>
  <tr id="<%= transport.id %>" class="<%= cycle("odd", "even") %>">
    <td><%= transport.name %></td>
  </tr>
  <% end %>
  </tbody>
</table>

<p class="stats"><%= number_with_delimiter(@transports.length) %> Transports</p>

<script type="text/javascript" src="/javascripts/jquery-<%= JQUERY_VERSION %>.min.js"></script>
<script type="text/javascript" src="/javascripts/jquery.tablesorter.min.js"></script>
<script type="text/javascript">
$('table').tablesorter({
  sortList: [[0,0]],
  widgets: ['zebra']
});
$('button').click(function() {
  $(this).find('a:first').click();
});
$('tbody tr').click(function() {
  $('tr').removeClass('selected');
  $(this).addClass('selected');
})
$('tbody tr').dblclick(function() {
  window.location = '/transports/' + $(this).attr('id') + '/edit';
});
</script>